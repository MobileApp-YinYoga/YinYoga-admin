<!-- res/drawable/edittext_error_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Nền trắng -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_red_dark" /> <!-- Viền đỏ -->
    <corners android:radius="16dp" /> <!-- Bo góc cho đẹp hơn -->

    <padding
        android:left="10dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="0dp" /> <!-- Tạo khoảng trống bên trong viền -->
</shape>
